{
  "name": "node-tide",
  "version": "0.2.3",
  "description": "A Node module to assist with connections to the Tide business banking API",
  "author": "Simon Skinner <simon@sisk-ltd.co.uk>",
  "repository": {
    "type": "git",
    "url": "https://github.com/vultuk/node-tide"
  },
  "main": "./dist/index.js",
  "typings": "./dist/index.d.ts",
  "scripts": {
    "compile": "rimraf dist && tsc -d",
    "example": "npm run compile && node ./dist/example.js",
    "prepublish": "npm run compile"
  },
  "dependencies": {
    "moment": "^2.18.1",
    "request": "^2.81.0",
    "request-promise-native": "^1.0.4"
  },
  "devDependencies": {
    "@types/node": "^7.0.32",
    "@types/request": "^2.0.0",
    "rimraf": "^2.5.4",
    "tslint": "^5.4.2",
    "typescript": "^2.4.0"
  }
}
