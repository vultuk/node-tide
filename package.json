{
  "name": "node-tide",
  "version": "0.0.1",
  "description": "A Node modules to assist with connections to the Tide business banking API",
  "repository": {
    "type": "git",
    "url": "https://github.com/vultuk/node-tide"
  },
  "main": "./dist/index.js",
  "typings": "./dist/index.d.ts",
  "scripts": {
    "compile": "rimraf dist && tsc -d",
    "prepublish": "npm run compile"
  },
  "dependencies": {
    "moment": "^2.18.1",
    "request": "^2.81.0"
  },
  "devDependencies": {
    "@types/node": "^7.0.32",
    "rimraf": "^2.5.4",
    "tslint": "^5.4.2",
    "typescript": "^2.4.0"
  }
}
